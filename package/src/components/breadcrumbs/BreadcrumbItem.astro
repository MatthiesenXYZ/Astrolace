---
import type { SlBreadcrumbItem } from '@shoelace-style/shoelace';

interface Props extends Omit<SlBreadcrumbItem, any> {
    /**
     * Optional URL to direct the user to when the breadcrumb item is activated. When set, a link will be rendered
     * internally. When unset, a button will be rendered instead.
     */
    href?: string;
    /** Tells the browser where to open the link. Only used when `href` is set. */
    target?: '_blank' | '_parent' | '_self' | '_top';
    /** The `rel` attribute to use on the link. Only used when `href` is set. */
    rel?: string;
}

const { href, rel = "noreferrer noopener", target, ...rest } = Astro.props;

---

<sl-breadcrumb-item {href} {rel} {target} {...rest}>
    <slot />
</sl-breadcrumb-item>

<script>
    import '@shoelace-style/shoelace/dist/components/breadcrumb-item/breadcrumb-item.js';
</script>